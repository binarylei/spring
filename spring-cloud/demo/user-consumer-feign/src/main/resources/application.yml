spring:
  application:
    name: user-consumer-feign
server:
  port: 10303
eureka:
  client:
    service-url:
      defaultZone: http://peer1:10011/eureka
  instance:
    prefer-ip-address: true     # 在应用中显示 IP
# 配置日志
logging:
  level:
  com.github.binarylei.springcloud.feign.service.FeignClientService: debug
