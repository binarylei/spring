server:
  port: 10500
spring:
  application:
    name: springcloud-config-server

  # spring cloud 通配符匹配：{application} ???
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/binarylei/{application}

